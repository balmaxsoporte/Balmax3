<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patiobalmax">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_MEDIA_STORAGE" android:minSdkVersion="29"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">
        
        <activity
            android:name=".LoginEstacionamiento"
            android:label="@string/titulo_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MapaEstacionamiento"
            android:label="@string/titulo_mapa_estacionamiento" />

        <activity
            android:name=".PatioEstacionamiento"
            android:label="@string/titulo_detalle_patio" />

        <activity
            android:name=".EditarPatioEstacionamiento"
            android:label="@string/titulo_editar_puesto" />

        <activity
            android:name=".AdministrarUsuarios"
            android:label="@string/titulo_administrar_usuarios" />

    </application>
</manifest>
